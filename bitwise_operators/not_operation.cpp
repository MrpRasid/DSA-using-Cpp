#include<iostream>
using namespace std;
int main(){
    // not operation
    // not answer will be let Number is n then : -(n+1);
    int a;
    cout<<"Enter a number for not operation: ";
    cin>>a;
    cout<<"Not is: "<<(~a);
}