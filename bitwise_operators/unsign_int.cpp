#include<iostream>
using namespace std;
int main(){
    // for unsigned value this return a large number value

    unsigned int num =-5;

    int bit =1;
    cout<<(num<<bit)<<endl;

    // for signed -ve value compile handel the value.

    int a =-5;
    cout<<(a>>1);

}